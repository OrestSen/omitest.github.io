import { StyleSheet } from 'react-native'

export const authLoadingScreenCss = StyleSheet.create({
    container: {
        height: '100%',
        alignItems: 'center',
        justifyContent: 'center'
    }
})